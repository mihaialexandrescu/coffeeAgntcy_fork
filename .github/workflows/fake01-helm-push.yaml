name: Fake Helm Push Charts

on:
  push:
    branches: [ghi223/test01, ghi223/test03-*]
    tags: ["test-tag-*"]
    paths:
      - 'coffeeAGNTCY/coffee_agents/*/deployment/helm/**'
  pull_request:
    branches: [ghi223/*]
    paths:
      - 'coffeeAGNTCY/coffee_agents/*/deployment/helm/**'
  workflow_dispatch:
    inputs:
      push:
        description: 'Whether to push the charts to the registry.'
        required: false
        type: boolean
        default: false
      build_corto:
        description: 'Whether to build the corto chart.'
        required: false
        type: boolean
        default: true
      build_lungo:
        description: 'Whether to build the lungo chart.'
        required: false
        type: boolean
        default: true

env:
  CORTO_DIR: coffeeAGNTCY/coffee_agents/corto
  LUNGO_DIR: coffeeAGNTCY/coffee_agents/lungo
  HELM_SUBDIR: deployment/helm

jobs:
  subprojects-to-build:
    runs-on: ubuntu-latest
    outputs:
      build_corto: ${{ steps.check-subprojects.outputs.build_corto }}
      build_lungo: ${{ steps.check-subprojects.outputs.build_lungo }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Log event info
        run: |
          echo "event_name=${{ github.event_name }}"
          echo "ref=${{ github.ref }}"
          echo "ref_name=${{ github.ref_name }}"

      - name: Log dispatch inputs
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          echo "push=${{ inputs.push }}"
          echo "build_corto=${{ inputs.build_corto }}"
          echo "build_lungo=${{ inputs.build_lungo }}"

      - name: Detect changed paths for main sub-projects
        id: check-subprojects
        run: |
          build_corto=false
          build_lungo=false

          if [[ "${{ github.event_name }}" == "push" ]]; then
            build_corto=true
            build_lungo=true
          elif [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            build_corto=${{ inputs.build_corto }}
            build_lungo=${{ inputs.build_lungo }}
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            base="${{ github.event.pull_request.base.sha }}"
            head="${{ github.event.pull_request.head.sha }}"

            # Returns 0 if dir has changes, 1 if no changes
            dir_has_changes() (
              local base=$1 head=$2 dir=$3
              set -x # This option only lives in the subshell of this function because the function is defined with () instead of {}.
              ! git diff --name-only --exit-code --ignore-blank-lines $base $head "$dir"
            )
            dir_has_changes $base $head "$CORTO_DIR/$HELM_SUBDIR" && build_corto=true || build_corto=false
            dir_has_changes $base $head "$LUNGO_DIR/$HELM_SUBDIR" && build_lungo=true || build_lungo=false
          fi

          echo "build_corto=$build_corto" | tee -a $GITHUB_OUTPUT
          echo "build_lungo=$build_lungo" | tee -a $GITHUB_OUTPUT


  helm-package-corto:
    needs: subprojects-to-build
    if: ${{ needs.subprojects-to-build.outputs.build_corto == 'true' }}
    strategy:
      matrix:
        chart:
          - name: corto-ui
            path: coffeeAGNTCY/coffee_agents/corto/deployment/helm/corto-ui
            package_name: corto-ui
    permissions: # permissions can only be maintained or reduced (not elevated) throughout a chain of workflows
      packages: write
      contents: read
    uses: ./.github/workflows/fake01-reusable-helm.yaml
    with: &helm-package-common-inputs
      path: ${{ matrix.chart.path }}
      package_name: ${{ matrix.chart.package_name }}
      push: ${{ github.event_name == 'push' || ( github.event_name == 'workflow_dispatch' && inputs.push ) }}
      # push: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/heads/ghi223')) }}

  helm-package-lungo:
    needs: subprojects-to-build
    if: ${{ needs.subprojects-to-build.outputs.build_lungo == 'true' }}
    strategy:
      matrix:
        chart:
          - name: lungo-ui
            path: coffeeAGNTCY/coffee_agents/lungo/deployment/helm/ui
            package_name: lungo-ui
    permissions: # permissions can only be maintained or reduced (not elevated) throughout a chain of workflows
      packages: write
      contents: read
    uses: ./.github/workflows/fake01-reusable-helm.yaml
    with: *helm-package-common-inputs
